name: Deploy to Production

on:
  push:
    branches: ["main"]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  # NOTE: Self-hosted deployment is currently handled manually via deploy.sh
  # To re-enable auto-deploy, register a self-hosted runner on your server and
  # uncomment the deploy job below, replacing 'self-hosted' with your runner label.
  #
  # deploy:
  #   runs-on: self-hosted
  #   needs: build-and-push
  #   steps:
  #     - name: Deploy to Local Server
  #       run: |
  #         cd /opt/vastiva
  #         docker pull ghcr.io/${{ github.repository }}:latest
  #         docker compose down && docker compose up -d
  #         docker image prune -f
